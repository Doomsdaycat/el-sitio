apiVersion: v1
kind: Service
metadata:
  name: {{ include "el-sitio.fullname" . }}
  labels:
    {{- include "el-sitio.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "el-sitio.selectorLabels" . | nindent 4 }}
